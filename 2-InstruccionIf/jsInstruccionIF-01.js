function mostrar() {
	var edad;

	edad = txtIdEdad.value;

	edad = parseInt(edad);

	// Operadores relacionales: (==), <, >, !=, <=, >=

	if (edad==15) 
	{ //Condicional simple 
		alert("Niña bonita");
	}
}//FIN DE LA FUNCIÓN