/*
Debemos lograr tomar un nombre con 'prompt' 
y luego mostrarlo por 'alert' al presionar el botón  'mostrar'*/
function mostrar()
{
	//La variable debe llevar un nombre representativo del dato que se guarda en ella.
	var dato; // Notacion camel case - todas las variables que escribo deben ir en minuscula.
	dato=prompt("Ingrese un dato."); // Para guardar datos en la variable usar el operador de inicializacion "=".	
	alert(dato);// Aca termina el ejercicio

	number = 1; // number
	var texto;
	texto = "Hola" // Hola
	boolean = true // boolean
	variableSinValor = null // null - Cuando explicitamente queremos que la variable no tenga valor asignado
	//undefined: cuando no se le asigna valor a una variable. Nunca se inicializo 
	//var nombreCompleto - Notacion camel case: si la variable es compuesta, las siguientes palabras deben ir en mayuscula.

	//typeof(nombre); | Indica de que tipo es la variable.
}

